pipeline {
 agent any
 environment{
  var_id = '$number'
  number = 'String onPremiseid'
  }
  
  stages {
    stage('Build') {
     steps { 
          echo "print the value of $var_id"
	  echo "what is $number"
	  }
     }
   }
}   
  
	  
