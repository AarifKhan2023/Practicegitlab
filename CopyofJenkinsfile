//Pipeline
    node{
       environment{
            var_id = '$number'
            number = 'onPremiseid'
        }
	stages{
	 stage('Build') {
	  steps {
	   echo "what is the $var_id"
	   }
	  }
	}
   }
}
	  
