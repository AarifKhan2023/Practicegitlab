try{
    node(node_name){
        environment{
            var_id = '$number'
            number = {Default Value, (String onPremiseid) }
        }
	stages{
	 stage('Build') {
	  steps {
	   echo "what is the $var_id"
	   }
	  }
	}
   }
}
	  
